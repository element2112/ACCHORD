async function create_playlist(spotifyToken, name) {
  const res = await fetch(`https://api.spotify.com/v1/me/playlists`, {
    method: 'POST',
    headers: { 'Authorization': 'Bearer ' + spotifyToken, 'Content-Type': 'application/json'},
    body: JSON.stringify({
      "name": "acchord playlist : " + name,
      "description": "playlist generated by acchord",
      "public": true
    })
  })

  return res.json();
}

export default create_playlist
